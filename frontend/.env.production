# Production Environment Variables

# Backend API URL - uses relative path for production
# The web server (nginx/apache) should proxy /api to the backend
REACT_APP_API_URL=https://nutripot.in/api

# AWS S3 Configuration
REACT_APP_AWS_REGION=ap-south-1
REACT_APP_S3_BUCKET_NAME=thrayam-threads-s3
